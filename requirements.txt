# === Lot 0 — Fondations ===
pydantic>=2.5,<3
PyYAML>=6.0
typer>=0.12.0
rich>=13.7.0

# Développement / tests
pytest>=8.0.0

# === Lot 1 — couche données ===
httpx>=0.27          # appels API Binance (klines, exchangeInfo, ticker/24hr)
pyarrow>=16.0        # cache parquet
pandas>=2.2          # manipulation OHLCV

# === Lots suivants (à décommenter le moment venu) ===
# numpy>=2.0           # Lot 2
# TA-Lib>=0.6.0        # Lot 2 : indicateurs (wheels officiels, incl. Windows)
# anthropic>=0.40      # Lot 5 : synthèse fondamentale (API Anthropic)
# streamlit>=1.37      # Lot 6 : dashboard (option)
