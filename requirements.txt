# === Lot 0 — Fondations ===
pydantic>=2.5,<3
PyYAML>=6.0
typer>=0.12.0
rich>=13.7.0

# Développement / tests
pytest>=8.0.0

# === Lot 1 — couche données ===
httpx>=0.27          # appels API Binance (klines, exchangeInfo, ticker/24hr)
pyarrow>=16.0        # cache parquet
pandas>=2.2          # manipulation OHLCV

# === Lot 2 — indicateurs ===
numpy>=2.0           # tableaux float64 contigus pour TA-Lib
TA-Lib>=0.6.0         # indicateurs (wheels officiels, incl. Windows) ; aucun réimplémenté à la main

# === Lot 5 — fondamental (Mode B) ===
anthropic>=0.68        # synthèse fondamentale (API Anthropic, claude-sonnet-5 + recherche web)
python-dotenv>=1.0     # chargement de .env (clés API, jamais en dur)

# === Lots suivants (à décommenter le moment venu) ===
# streamlit>=1.37      # Lot 6 : dashboard (option)
